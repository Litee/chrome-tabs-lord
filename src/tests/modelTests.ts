// <reference path="../scripts/model.ts" >
import {Model} from '../scripts/model';

describe('Model', () => {
  const model = new Model();
  it('show allow to add new tab', () => {
    expect(true).toBe(true);
  });
});
